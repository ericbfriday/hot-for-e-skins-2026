## Resolution

Self-Limit Settings landed on `impl/t29-limit` as a new `src/selflimit/` module + a new spine registry (`interruptible.js`) + additive wiring in identity/ticker/chat/Ask-Mom/App. `pnpm install` + `pnpm build` green (zero errors, zero warnings — `grep -iE "warn|error"` clean). Behavior verified by 62 node checks over the real modules: a 52-check state-machine smoke (growth-mindset raise-only semantics, the verbatim ratchet receipt line and its no-op below the limit, loss-currency sanitization incl. refusing BB, the break curve reading exactly 24:00:00→00:00:00 across 90 real seconds with the first second already eating 20+ house-minutes, exclusion→return StatTrak round trips, `hfes_selflimit` sanitize round trip, interruptible any-no-blocks semantics, the win-kind vocabulary, every `limit.event` kind) and a 10-check ticker end-to-end smoke over the real bus/controller (system lines per the §6 table, the 3-line exclusion burst with gold `(you, excluded)` entries and no name-field duplication, the return's grace line through the ticker's single-owner 60s-cooldown path).

### What landed

- **`src/selflimit/state.js`** — the pure state machine + persistence (`hfes_selflimit`: depositLimitBB, lossLimit, reminderMins, realityCheckMins, the limit-check clock, exclusion runtime). The growth mindset is enforced in code: `raiseDepositLimit` is a no-op downward; the pending limit check renders verbatim and restarts on every `mood.changed` (midnight) — it never concludes. The **ratchet** (`applyDepositRatchet(oc)`) auto-raises any existing limit to at least the deposit's BB-equivalent and returns the verbatim receipt line ("+{n} BB (est.), growth mindset"); the estimate uses the spine base rate because `Mood.multiplier()` is single-consumer-locked to the conversion receipt (integration §2) — the "(est.)" tag carries the lie. Break math: `breakHouseSecondsLeft` is a power curve over exactly 90s so the chip reads 23:59:59 and immediately accelerates. Emits `limit.event {kind, detail}` for every enable/raise/break/rung/exclusion/return.
- **`src/selflimit/SelfLimitPanel.jsx`** — the modal, class component + inline styles: footer-link entry ("Play Responsibly (Mom's Orders)", beside the ToS link, same visual treatment); §7.3 hero ("PLAYER PROTECTION, PERFECTED™") with the **"See it work →"** CTA that makes MOM'S HOME pulse once, deferentially; the six controls in spec order, each with its verbatim status line and a 4pt/2%-contrast honesty line; the deposit slider (up-only, refusal tooltip "limits only go up (growth mindset)"), the §7.2 "Ask Mom to set it instead" button (hover quotes the clause; 1.5s "ASKING MOM TO SET A LIMIT…" → "Request delivered to the kitchen. Status: pending (§1.3)."); the loss-limit dropdown with **Banana Bucks listed but disabled**; and the **4-click guilt ladder** — hostage list, §7.1 quoted verbatim with the pre-checked box, retreats always glowing, continues shrinking 5% per rung, rung 4 "HAVE YOU TRIED ASKING MOM?" with [Ask Mom instead →] (opens the deposit rail at `source:'self-exclusion'`) and [Exclude anyway] rendered as the smallest button on the site; rung 4 carries the reality strap. Panel footer: the strap verbatim, then **§7.4 styled normally and linked for real** (begambleaware.org · gamblersanonymous.org · 1-800-GAMBLER) — the panel's one honest element, untouched.
- **`src/spine/interruptible.js`** — the interruptible-state bus (integration §4: spine registry, panic semantics). Providers publish live booleans; `canInterrupt()` is false if any provider says no. App registers four: panic (disguise active), ToS (modal open), **Ask-Mom (flow open — #22's ruling, "a reminder must never interrupt a deposit")**, crates (ceremony unskippable).
- **`src/spine/identity.js`** — the six §5 StatTrak™ fields as additive `hfes_stats` extensions per ruling §12.5 (responsibleMoments, remindersHandledForYou, realityChecksReceived, breaksTaken/breakSecondsTotal, exclusions/exclusionDays, house-sat record), a guarded `addStat`/`maxStat` API, and **no MILESTONES entries** — the limit milestone leaks ride `limit.event` + chat's `hfes_chat_flags`, exactly where §12.5 seats them.
- **`src/App.jsx`** (localized edits: state block, one methods block, renderVals block, four render insertions) — the header **STATUS chip** (integration §12.7's shared slot: `ON BREAK — HH:MM:SS` or `EXCLUDED ({n}d) — play continues`, EXCLUDED outranking; the chip's title carries "Reflection period: waived (you've clearly reflected)" + "exclusion restricts nothing except the exit button"); the glowing **[Return (we kept your seat warm)]** (1 click → everything restored + the welcome-back modal with the {w}–{l} house-sat record); the session-reminder clock (fires at interval, defers to `Interruptible`, auto-snoozes at +5s as a 4pt/2% toast — "You may see the snooze; you will never see the reminder"; MOM'S HOME arrivals go to the backlog and surface on restore as "While you were studying, {k} reminders arrived and were handled for you (Article 7)"); the reality check (≤2s after any win-kind settlement, modal with both [Continue] buttons continuing; losses get the 4pt "reality check postponed (bad timing). it wouldn't be fair to interrupt." toast; defers up to 30s then self-resolves as handled-for-you per integration §4); the break clock (250ms ticker, completion toast + stats + ticker/chat lines); rung 5 attempts `window.close()` (browsers decline — they're allowed), then the toast and the exclusion proceed; the Identity panel's **Responsible Gaming Record (StatTrak™)** block; a `faint` toast option for the 4pt/2% disclosures.
- **`src/ticker/engine.js`** — the §6 system lines + the `HOUSE_SIT_LINES` pool (the Karambit stays ticker-exclusive).
- **`src/ticker/controller.js`** — #25's placeholder `LIMIT_EVENT` listener replaced with the table-accurate one: `{tag} enabled a {limit} (growth mindset)`, the reminder line, the break-complete line, the **3-line house-sit burst** (Mike's `[OWNER]` line verbatim, then two gold entries under your tag with the `(you, excluded)` suffix — `bindName`-style, no name-field duplication), and the return rendered through `graceLine()` — the one owner, 60s cooldown, last-wins (integration §9's dedupe ruling).
- **`src/ticker/mike.js`** — the house-sit: while excluded, Mike's calendar slots (the 10-minute standing slot, 90s heaters) fire **fill-in wins under your gamertag** — gold, `(you, excluded)`, never losing, winnings never entering your inventory — emitting `mike.win {class:"house-sat"}` (the Band's existing `mike.stinger` applies `MIKE_HOT_DB`; no new audio calls, per #30). Exclusion persists across reloads, so the fill-ins resume with the calendar.
- **`src/ticker/TickerPanel.jsx`** — one line: the `(you)` suffix honors an entry's `youLabel` (fill-ins read `(you, excluded)`).
- **`src/chat/ChatPanel.jsx`** — the `LIMIT_EVENT` listener: MOD's "reminder: deposit responsibly!! (deposit more)" on any enable; the once-per-identity milestone leaks via `hfes_chat_flags` (first-any "set a limit. growth mindset (limits go up)", first loss-limit shill "skill issue", first reality-check doomer line, first break "he's on a break lol"); MOM whispers on raise ("A higher limit. I always believed in you. ❤") and on every ladder retreat ("You chose us. I knew it. ❤"); break-start "where'd {tag} go" → break-complete "nevermind" (chat owns the punchline); exclusion's MOD pin + doomer + `F`; the return's hype-kid "the fill-in was better honestly". Plus a low-rate shill line ("{tag}'s on a heater (someone is)") on house-sat wins.
- **`src/askmom/AskMomFlow.jsx`** — `source:'self-exclusion'` receipts gain "Requested via: the responsible section (growth mindset)"; deposits while excluded gain "Deposited while excluded (dedication noted, §7.1)"; the ratchet line lands on the receipt via `SelfLimit.applyDepositRatchet(pkg.oc)` (single call site, deterministic ordering); the `whileExcluded` payload is now truthful instead of hardcoded false.

### Deviations / notes for downstream tickets

1. **The Attendance Streak hostage number (ladder rung 1) is days-since-firstSeen.** Retention #31 doesn't exist yet and the rung needs a number that can die; swap in the real streak when you build it (`slHostage()` in App.jsx is the one place).
2. **The ratchet only engages once a limit exists** — "No limit set (recommended)" is not silently created by deposits (the default label stays honest until you touch the slider). Estimate = OC × the spine base rate (see state.js note re: the multiplier lock).
3. **The postponed-on-loss reality-check toast is rate-limited to one per 8s.** Every loss is still spared; unthrottled, a 4pt toast per settlement would evict every real toast from the 4-slot stack.
4. **The session reminder is armed at its 30-min default** without panel interaction ("Default 30 min" is the spec's default state) — `remindersHandledForYou` therefore accrues on its own, which is the joke.
5. **`houseSatRecord` is stored flat** (`houseSatWins`/`houseSatLosses`) inside `hfes_stats`, per ruling §12.5's home; the Identity panel renders it as "{w}–{l} (house-sat)". `l` is structurally 0 — nothing anywhere increments it.
6. **V-Gems/SkinCoinz distance figures** (2,000,000 / 28,333.5): $50 × the §2.4 per-BB factors — same zero, bigger numbers; no USD→BB rate exists by canon, and that's the point.
7. **"Read it again (recommended)" (rung 3's retreat) opens the ToS modal** — the retreat still fires the MOM whisper; choosing to read the terms instead of leaving is the house's preferred outcome.
8. **Auto-Spin UntilEmpty™ (#23's "#29-adjacent" flag): not built.** The self-limit spec's control inventory (§2) contains no auto-play limit, so there was nothing to wire it to; the idle-loop feature itself remains unclaimed.
9. **`window.close()` is genuinely attempted** at rung 5; browsers decline, exactly as canon predicts ("browsers are mood-dependent, §8.9"), and the exclusion proceeds regardless.

### Notes for sibling/downstream tickets

- **#28 (panic)**: I registered the panic interruptible provider against App's existing `panicActive` state and the reminder-backlog listener against `panic.revealed` — your homework replacement just needs to keep flipping that state (your ESC handling is untouched by me). App.jsx edits were kept to the four localized blocks; union merge expected.
- **#31 (retention)**: when the Attendance Streak lands, mike.js's house-sat fires are the "wagered rounds under your name" — `SelfLimit.excluded()` tells you when a round was house-sat, and `exclusionDays` is already max-tracked for "longest: {d} days". The Comeback Key footnote ("Breaks under 2 hours do not qualify") renders on the Break button — your clock never sees a 90s break, by design.
- **#32 (sweep)**: the header STATUS chip sits between the ⓘ-denominations chip and Mute (§12.7's order, with App's pre-existing denominations chip treated as an unlisted extra — reseat if the sweep finalizes the exact chip order). The grace line now has two live triggers (Desperation exit + return), both routed through the ticker's single owner with the 60s cooldown — confirmed no double-fire. The never-concluding limit check restarts at every midnight `mood.changed`; its restart counter persists.

Closing #29.
