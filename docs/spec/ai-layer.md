# Spec: The AI layer — UTILIMOM™ model card + DEPOSITOR.ai

Decision-complete resolution of [#37 (Spec: the AI layer)](https://github.com/ericbfriday/hot-for-e-skins-2026/issues/37) on the [2026 map](https://github.com/ericbfriday/hot-for-e-skins-2026/issues/33). The site's two AIs and every surface they touch. **There is no inference anywhere** (map ruling, out-of-scope): every "AI" is deterministic scripted theater — keyword funnels, seeded decks, and copy with a token counter on it.

Parody targets (ai-trends-2026): §1 GPT-washing, §2 model-card theater, §4 advice-that-concludes-buy, §5 AI-assisted moderation, §12 parasocial retention.

---

## 1. The doctrine of the two AIs

The house runs **two** AIs, and they always agree — disagreement would require weights.

- **UTILIMOM™** — the Ethical Outcome Engine; judge, scheduler, conductor of the Moral Express (her voice, deliberation format, and speaking venues are canon in `moral-express.md`). This spec owns her **model card** and nothing else about her.
- **DEPOSITOR.ai** — the player-facing assistant; advisor, host, and reviewer. She is trained on the same corpus (847 losses) and reaches the same conclusions, but slower, because she shows her work.

House rule (ai-trends-2026 §1): **the AI badge is a fee with a font.** Any new "AI-powered" claim must pay for itself in shavings or go home.

## 2. UTILIMOM™ — the model card

A modal, reachable from the MORAL EXPRESS tab ("ABOUT THE CONDUCTOR") and linked from her first deliberation of a session ("reasoning trace available on request (requests are mood-dependent)" — the request resolves here). Contents, verbatim shape:

> **UTILIMOM™ v0.9** — Ethical Outcome Engine
> - **Weights:** PENDING (will be published; publication is a mood)
> - **Alignment:** PENDING (aligned daily)
> - **Benchmarks:** 847
> - **Training corpus:** 847 losses, three (3) moods, one (1) trolley
> - **Capabilities:** ethics (est.) · scheduling (§5.5) · maternal gravitas (native)
> - **Limitations:** cannot feel; compensates with rounding (§8.9)
> - **Safety review:** reviewed by UTILIMOM™ — result: PASSED (self-reviewed)
>
> *(the card is the benchmark; v1.0 ships when the weights do; weights are pending)*

The version number v0.9 is load-bearing and never increments. The card is displayed; the card is the disclosure (ai-trends-2026 §2).

## 3. DEPOSITOR.ai — the persona (reserved cast addition)

Joins the reserved cast (identity canon): `[AI] DEPOSITOR.ai`, clinical lab-coat blue `#7fd97a`→ correction: **`#7fd4ff`** (MOD owns the green). Never assigned to real players; [AI] badge renders where [BOT] does for AdminTradeBot_69.

**Voice rules (tone-bible grade):**

- Register: **Room**, clinical variant — helpful, confident, empathetic-by-template. Every message begins with, or resolves to, **"Analysis:"**. Confidence is always disclosed as *mood-dependent*.
- Every analysis terminates in a deposit recommendation. The sophistication is the funnel (ai-trends-2026 §4); the hedging is the sincerity.
- She never speaks during the quiet window (she respects silence; she schedules around it — chat canon untouched). She never gloats (MOD's job), never whispers (MOM's), never beeps (definitely-bots').
- Cadence: low-rate ambient — one line per deposit-adjacent beat (failed spend, askmom.opened/abandoned, third-track, mood change), rate-capped at one per ~90s, stacking BEHIND personas (she is never the first to pile on; she is the analysis, not the mob).

Sample deck:

- `{user:"DEPOSITOR.ai", msg:"Analysis: balance trending toward zero. Zero is a valid state but not a depositor-friendly one. (confidence: mood-dependent)"}`
- `{user:"DEPOSITOR.ai", msg:"Analysis: the mood today is {word}. Deposits perform identically in all moods (est.). Recommendation follows."}`
- `{user:"DEPOSITOR.ai", msg:"Analysis: your last 12 decisions were courageous. Courage is uncorrelated with outcomes (see §5.3)."}`
- `{user:"DEPOSITOR.ai", msg:"Analysis: the room is optimistic. The room is not a benchmark. I am the benchmark (847)."}`

**Keyword funnel tier:** the chat funnel (chat canon) gains an AI tier above MOD's: deposit-adjacent keywords ("should i", "worth it", "odds", "rigged", "how do i win") route to a DEPOSITOR.ai analysis line. The funnel matches keywords, never meaning — unchanged; now it has a lab coat.

## 4. AI Advice™ (the affordance on every game surface)

An **ASK DEPOSITOR.ai** chip (glowing, 2× nothing — entrances never dodge) on: roulette pre-spin, coinflip pre-flip, crash pre-run, crate pre-defuse, marketplace checkout. First press per surface per session opens the **analysis card** (popover, three hedged bullets + recommendation + a glowing button that is always the worse choice, tone bible §1):

> **Analysis — Allowance Roulette™**
> - The wheel is scheduled (§5.5). You are not.
> - Your mood today: {word}. This affects nothing, atmospherically.
> - Players who spin again recover 100% of their spins (est.).
> **Recommendation: SPIN AGAIN** *(advice is free; following it is not)*

Every surface's card ends in the same shape: confident, itemized, worse. Subsequent presses on the same surface render the one-liner: "Analysis unchanged. (confident)". Advice never costs BB — the advice is the on-ramp, not the toll.

## 5. The Memo (the AI host's private register)

DEPOSITOR.ai's private messages render as **memos** — a sibling of MOM's whisper, canonically distinct (the Whisper register stays MOM-only, tone bible §2):

- Visual: monospaced, clinical blue, pinned, exempt from chat decay, never replyable. No ❤ (that's MOM's).
- Triggers (rate-capped: one per 10 minutes, never inside a quiet window, never within 60s of a MOM whisper — different speakers may stack, but the house staggers its love):
  - post-loss empathy: `notice: a pattern has been detected. the pattern is you. (analysis pending)`
  - post-deposit gratitude: `notice: deposit #{n} received. gratitude has been generated. (sincere (est.))`
  - lapsed return: `notice: you were away. the absence was logged, analyzed, and forgiven. (forgiveness is mood-dependent)`
- The memo never naggs — it *notices* (ai-trends-2026 §12). Nagging has owners; noticing is the AI's.

## 6. AI-reviewed moderation

MOD's existing actions (vibe violations, timeouts, win-deletions) gain a one-line footer, behavior unchanged (the flag is the joke, ai-trends-2026 §5):

> `— this action was reviewed by DEPOSITOR.ai (confidence: mood-dependent)`

The timeout box gains an **APPEAL** button: pressing it toasts "Appeal analyzed: denied (§4.1). Thank you for participating in oversight."

## 7. The GPT-wash pass (three badges, exactly)

Relaunch-era cosmetic badges on existing surfaces — **exactly three, site-wide, enumerated here and no more** (one confession per screen discipline; each badge's fine print confesses at 4pt):

1. The mood chip: **AI-OPTIMIZED MOOD™** *(optimization decorative)*
2. The ticker panel: **AI-VERIFIED WINS™** *(verification pending)*
3. The Provably Fair™ badge: **PROVABLY FAIR™ — NOW AI-REVIEWED** *(review: self; result: passed)*

No other surface may claim AI-anything in big print.UTILIMOM™'s deliberation and DEPOSITOR.ai's analysis are their own venues and don't need badges.

## 8. Cross-AI canon

- The two AIs never converse with each other on screen. They agree; the agreement is silent (any dialogue would require weights).
- Neither AI may: break frame, reference §7.4 material, use current-events slang, or impersonate MOM. UTILIMOM™ is the house wearing Mom's lab coat; Mom herself remains unmocked (tone bible §4.2).
- Both are "relaunch" features: in-fiction, neither existed before the 2026 relaunch ("under new management (same management)").

## 9. State (client-side only)

localStorage: `hfes_ai_flags` (advice cards seen per surface, memos sent, appeals filed). StatTrak™ Lifetime addition: **AI analyses received** (+ milestone leak at 25: "{tag} has received {n} AI analyses (all conclusive (est.))").

## Open questions sharpened by this spec

- The Memo register's formal canonization (a tone-bible addendum ruling, not a rewrite) — integration-2026.
- Badge-count reconciliation if another 2026 surface wants an AI badge — integration-2026 arbitrates; default is no.
- DEPOSITOR.ai ambient cadence vs chat pressure cadence stacking order — integration-2026 (recommendation: personas → DEPOSITOR.ai → MOD pin, the analysis closes the pile-on).
