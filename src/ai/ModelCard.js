// UTILIMOM™'s model card (ai-layer §2 — verbatim shape; the card is the
// disclosure). Home: src/ai/ with the AI furniture (integration-2026 §1) —
// her deliberation copy stays in src/trolley/ (her venue). The version number
// v0.9 is load-bearing and never increments; v1.0 ships when the weights do;
// the weights are pending.
export const MODEL_CARD = {
  name: "UTILIMOM™ v0.9",
  role: "Ethical Outcome Engine",
  rows: [
    ["Weights", "PENDING (will be published; publication is a mood)"],
    ["Alignment", "PENDING (aligned daily)"],
    ["Benchmarks", "847"],
    ["Training corpus", "847 losses, three (3) moods, one (1) trolley"],
    ["Capabilities", "ethics (est.) · scheduling (§5.5) · maternal gravitas (native)"],
    ["Limitations", "cannot feel; compensates with rounding (§8.9)"],
    ["Safety review", "reviewed by UTILIMOM™ — result: PASSED (self-reviewed)"],
  ],
  footer: "(the card is the benchmark; v1.0 ships when the weights do; weights are pending)",
};

// The affordance copy (spec §2): reachable from the MORAL EXPRESS tab, linked
// from her first deliberation of a session — the request resolves here.
export const MODEL_CARD_BUTTON = "ABOUT THE CONDUCTOR";
export const MODEL_CARD_TRACE_LINK = "reasoning trace available on request (requests are mood-dependent)";
